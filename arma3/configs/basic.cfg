// Based on server network and updated for today's standards
MaxMsgSend = 640; // Maximum amount of messages per frame (default: 128)
MaxSizeGuaranteed = 1024; // (default: 512)
MaxSizeNonguaranteed = 512; // (default: 256)
MinBandwidth = 107374182; // ~100 Mb based on server bandwidth (value in bits)
MaxBandwidth = 1073741824; // ~1 Gb based on server bandwidth (value in bits)
MinErrorToSend = 0.001; // (default)
MinErrorToSendNear = 0.01; // (default)
MaxCustomFileSize = 0; // Disable custom faces
class sockets {
    maxPacketSize = 1400; // MTU (default: 1400)
    initBandwidth = 2000000; // 16 Mb (value in bytes, default: 32 Kb)
    MinBandwidth = 64000; // 512 Kb lowest DSL (value in bytes)
    MaxBandwidth = 2000000; // 16 Mb average DSL (value in bytes)
};

